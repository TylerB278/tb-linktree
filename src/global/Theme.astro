---
// we can import fonts here, after installing them via Fontsource
// see https://fontsource.org/docs/installation for instructions
import "@fontsource/karla";
---

<style is:global>
  /* We can use this style tag to set up colors, fonts, etc. */
  @import url('https://fonts.googleapis.com/css2?family=League+Spartan:wght@100..900&family=Libre+Franklin:ital,wght@0,100..900;1,100..900&family=Space+Mono:ital,wght@0,400;0,700;1,400;1,700&display=swap');

  :root {
    --color-background: rgb(0, 0, 0);
    --color-text: black;
    --color-accent: rgb(255, 230, 0);
    --color-focus-ring: rgb(255, 0, 255);

    --font-body: "Libre Franklin", sans-serif;
    --font-headings: "Libre Franklin", sans-serif;

    font-size: 18px;
    cursor: none;
    overflow: hidden;
    
    /* These sizes are used by the Container component to set up the page container */
    --size-page-width: 100%;
    --size-page-padding: 0rem;
  }

  /* We can also apply these colors as a default */
  body {
    background-image: url(/Portfolio-UnityBackground.png);
    background-size: cover no-repeat;
    background-color: var(--color-background);
    color: var(--color-text);
    font-family: var(--font-body);
  }
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    color: var(--color-text);
  }

  /* default focus ring styles */
  *:focus-visible {
    outline: 3px solid var(--color-focus-ring);
  }

</style>
